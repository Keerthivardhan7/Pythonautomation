import time
from tkinter import Radiobutton

from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from selenium.webdriver.common.by import *
from selenium.webdriver.support.select import Select

# chrome driver service selenium

driver = webdriver.Chrome()
# driver.get("https://rahulshettyacademy.com/dropdownsPractise/")
driver.get("https://rahulshettyacademy.com/AutomationPractice/")
driver.maximize_window()

name="Keerthi"

# how to create a script for java popups using selenium

driver.find_element(By.CSS_SELECTOR, '#name').send_keys(name)

driver.find_element(By.CSS_SELECTOR, '#alertbtn').click()

time.sleep(2)

# here comes the java pop up message so write script accordingly

# switch from driver to alert mode use --switch to alert()--- method
alert = driver.switch_to.alert
alertText = alert.text
print(alertText)

# to verify that name is displayed alert text use this assert
assert name in alertText

# like to click on Ok of pop button # accept method means positive means click on

alert.accept()

# ---- like to click on cancel option on alert use below script and click on Confirm button and click cancel in popup --

# driver.find_element(By.CSS_SELECTOR, '#name').send_keys(name)
driver.find_element(By.CSS_SELECTOR, '#confirmbtn').click()
time.sleep(2)
alert = driver.switch_to.alert
alertText = alert.text
print(alertText)

# assert name in alertText
alert.dismiss()





